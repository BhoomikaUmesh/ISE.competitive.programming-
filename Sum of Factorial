#include <stdio.h>
void main()
{
    int n,i,fact=1;
    float sum=0;
    printf("enter n value:");
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
        fact=fact*i;
        sum=sum+fact/i;
        
    }
    printf("%f",sum);
}
